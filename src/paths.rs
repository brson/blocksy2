use anyhow::Result;
use std::path::{Path, PathBuf};

pub fn tree_path(dir: &Path, tree: &str) -> Result<PathBuf> {
    panic!()
}

pub fn log_path(path: &Path) -> Result<PathBuf> {
    panic!()
}

pub fn commit_log_path(path: &Path) -> Result<PathBuf> {
    panic!()
}
